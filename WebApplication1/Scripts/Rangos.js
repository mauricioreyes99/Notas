function Rango1(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num=="") {

    } else {
        alert("fuera de rango");
        document.getElementById('a').value = '';
    }
}
function Rango2(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('b').value = '';
    }
}
function Rango3(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('c').value = '';
    }
}
function Rango4(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('d').value = '';
    }
}
function Rango5(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('e').value = '';
    }
}
function Rango6(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num=="") {

    } else {
        alert("fuera de rango");
        document.getElementById('f').value = '';
    }
}
function Rango7(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('g').value = '';
    }
}
function Rango8(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('h').value = '';
    }
}
function Rango9(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('i').value = '';
    }
}
function Rango10(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('j').value = '';
    }
}
function Rango11(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('k').value = '';
    }
}
function Rango12(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0 || num == "") {

    } else {
        alert("fuera de rango");
        document.getElementById('l').value = '';
    }
}